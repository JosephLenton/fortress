use util::colour::RGBA;

/// Black.
pub const BLACK: RGBA = RGBA {
    red: 0,
    green: 0,
    blue: 0,
    alpha: 255,
};

/// White.
pub const WHITE: RGBA = RGBA {
    red: 255,
    green: 255,
    blue: 255,
    alpha: 255,
};

/// Red, but light!
pub const LIGHT_RED: RGBA = RGBA {
    red: 250,
    green: 128,
    blue: 144,
    alpha: 255,
};

/// Normal red.
pub const RED: RGBA = RGBA {
    red: 255,
    green: 0,
    blue: 0,
    alpha: 255,
};

/// SHOCKING PINK!
pub const PINK: RGBA = RGBA {
    red: 255,
    green: 0,
    blue: 255,
    alpha: 255,
};

/// I hate purple.
pub const PURPLE: RGBA = RGBA {
    red: 128,
    green: 0,
    blue: 128,
    alpha: 255,
};

/// Brown as a stick.
pub const BROWN: RGBA = RGBA {
    red: 175,
    green: 90,
    blue: 35,
    alpha: 255,
};

/// Yellow.
pub const YELLOW: RGBA = RGBA {
    red: 255,
    green: 215,
    blue: 0,
    alpha: 255,
};

/// Dark grey.
pub const DARK_GREY: RGBA = RGBA {
    red: 75,
    green: 75,
    blue: 75,
    alpha: 255,
};

/// Is it grey or gray?
pub const GREY: RGBA = RGBA {
    red: 120,
    green: 120,
    blue: 120,
    alpha: 255,
};

/// Light grey.
pub const LIGHT_GREY: RGBA = RGBA {
    red: 180,
    green: 180,
    blue: 180,
    alpha: 255,
};

/// Isn't cyan already light?
pub const LIGHT_CYAN: RGBA = RGBA {
    red: 0,
    green: 255,
    blue: 255,
    alpha: 255,
};

/// Cyan, light, but not as light as light cyan.
pub const CYAN: RGBA = RGBA {
    red: 64,
    green: 224,
    blue: 208,
    alpha: 255,
};

/// Light blue.
pub const LIGHT_BLUE: RGBA = RGBA {
    red: 30,
    green: 144,
    blue: 255,
    alpha: 255,
};

/// SEGA BLUE SKY!
pub const BLUE: RGBA = RGBA {
    red: 0,
    green: 0,
    blue: 255,
    alpha: 255,
};

/// Light green.
pub const LIGHT_GREEN: RGBA = RGBA {
    red: 0,
    green: 255,
    blue: 0,
    alpha: 255,
};

/// Green.
pub const GREEN: RGBA = RGBA {
    red: 50,
    green: 205,
    blue: 50,
    alpha: 255,
};
